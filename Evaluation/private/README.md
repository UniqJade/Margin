# Private evaluation inputs

Place prepared datasets containing copyrighted reading selections in this
directory. Use a `.local.json` or `.private.json` suffix. The adjacent
`Evaluation/.gitignore` keeps those files out of Git.

Before every commit, still check `git status --short`. Do not rely on a filename
convention as the only protection for private reading data.
